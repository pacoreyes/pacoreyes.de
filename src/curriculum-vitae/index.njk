---
layout: page.njk
title: Curriculum Vitae
description: Juan-Francisco (Paco) Reyes's Curriculum Vitae
keywords: Juan-Francisco (Paco) Reyes, Paco Reyes, interaction designer, front-end, UI engineer
author: Juan-Francisco (Paco) Reyes
---
<h1>{{ title }}</h1>
{% block content %}
  <section id="personal-information">
    <h2>Personal Information</h2>
    <ul>
      <li>Name: {{ name }}</li>
      <li>Email: {{ email }}</li>
      <li>LinkedIn: {{ linkedin }}</li>
      <li>GitHub: {{ github }}</li>
    </ul>
  </section>

  <section id="summary">
    <h2>Professional Summary</h2>
    <p>{{ summary }}</p>
  </section>

  <section id="skills">
    <h2>Skills</h2>
    <ul>
      {% for skill in skills %}
        <li>{{ skill }}</li>
      {% endfor %}
    </ul>
  </section>

  <section id="experience">
    <h2>Professional Experience</h2>
    {% for job in jobs %}
      <div class="job">
        <h3>{{ job.title }}</h3>
        <p>{{ job.company }}, {{ job.location }}</p>
        <p>{{ job.start }} - {{ job.end }}</p>
        <ul>
          {% for duty in job.duties %}
            <li>{{ duty }}</li>
          {% endfor %}
        </ul>
      </div>
    {% endfor %}
  </section>

  <section id="projects">
    <h2>Projects</h2>
    {% for project in projects %}
      <div class="project">
        <h3>{{ project.name }}</h3>
        <p>{{ project.description }}</p>
        <p>Tech Stack: {{ project.techStack }}</p>
        <p>GitHub: {{ project.githubLink }}</p>
        {% if project.demoLink %}
          <p>Demo: {{ project.demoLink }}</p>
        {% endif %}
      </div>
    {% endfor %}
  </section>

  <section id="education">
    <h2>Education</h2>
    {% for degree in education %}
      <div class="degree">
        <h3>{{ degree.degree }}</h3>
        <p>{{ degree.university }}, {{ degree.location }}</p>
        <p>{{ degree.start }} - {{ degree.end }}</p>
      </div>
    {% endfor %}
  </section>

  <section id="publications">
    <h2>Publications</h2>
    {% for publication in publications %}
      <div class="publication">
        <p>{{ publication.title }}</p>
        <p>{{ publication.journal }}</p>
        <p>{{ publication.year }}</p>
        <p><a href="{{ publication.link }}">Link</a></p>
      </div>
    {% endfor %}
  </section>

  <section id="awards">
    <h2>Awards and Honors</h2>
    <ul>
      {% for award in awards %}
        <li>{{ award }}</li>
      {% endfor %}
    </ul>
  </section>

  <section id="languages">
    <h2>Languages</h2>
    <ul>
      {% for language in languages %}
        <li>{{ language }}</li>
      {% endfor %}
    </ul>
  </section>

  <section id="certifications">
    <h2>Certifications</h2>
    <ul>
      {% for certification in certifications %}
        <li>{{ certification.name }} - {{ certification.issuer }}</li>
      {% endfor %}
    </ul>
  </section>

  <section id="references">
    <h2>References</h2>
    <p>Available upon request</p>
  </section>
{% endblock %}
